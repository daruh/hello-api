apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: "/translate"
            backend:
              service:
                name: hello-api
                port:
                  number: 80